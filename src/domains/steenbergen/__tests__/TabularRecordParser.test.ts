import {fixIndices} from "../datafixers";
import PartOfSpeech = steenbergen.PartOfSpeech;

describe('fixIndices', () => {

    it('should not change index if the record is unique', () => {
        const records

    })


    function aRecord(lemmas: string, modifiers: string, partOfSpeech: PartOfSpeech,)
});